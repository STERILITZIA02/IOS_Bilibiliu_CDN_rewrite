{
  "name": "ios-bilibili-cdn-rewrite",
  "version": "2.0.0",
  "private": true,
  "description": "Conservative Bilibili routing, ad/UI filtering, and resource-safe CDN selection for Shadowrocket on iOS.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check",
    "test": "node --test --experimental-test-isolation=none",
    "check": "npm run build:check && npm test",
    "smoke:auto": "node scripts/smoke-auto.mjs"
  }
}
